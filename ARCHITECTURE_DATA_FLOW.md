# PostForge Scheduler - Architecture & Data Flow Diagram

## 📊 Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          USER (Browser)                                      │
│                     Chrome/Firefox/Safari/Edge                              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               │ HTTPS
                               │
┌──────────────────────────────▼──────────────────────────────────────────────┐
│                     FRONTEND LAYER (Next.js 16)                              │
│                     Port: 3003 (localhost)                                   │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                    CLIENT COMPONENTS (React)                        │   │
│  │                                                                      │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐            │   │
│  │  │  Dashboard   │  │    Posts     │  │   Generate   │            │   │
│  │  │  /dashboard  │  │/dashboard/   │  │/dashboard/   │            │   │
│  │  │              │  │   posts      │  │  generate    │            │   │
│  │  │ - Overview   │  │              │  │              │            │   │
│  │  │ - Stats      │  │ - Table View │  │ - AI Form    │            │   │
│  │  │ - Credits    │  │ - Calendar   │  │ - Preview    │            │   │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘            │   │
│  │         │                  │                  │                     │   │
│  │         │                  │                  │                     │   │
│  │  ┌──────▼──────────────────▼──────────────────▼───────────────┐   │   │
│  │  │            SHARED UI COMPONENTS                             │   │   │
│  │  │                                                              │   │   │
│  │  │  • PostsTable (Table with actions)                          │   │   │
│  │  │  • CalendarView (react-big-calendar + DnD)                  │   │   │
│  │  │  • EditPostModal (Dialog + Form)                            │   │   │
│  │  │  • CreditOverview (Stats cards)                             │   │   │
│  │  │  • Badge, Button, Dialog, Table (shadcn/ui)                 │   │   │
│  │  │  • SkeletonStatCard (Loading states)                        │   │   │
│  │  └──────────────────────────────────────────────────────────────┘   │   │
│  │                                                                      │   │
│  │  State Management: React useState + useEffect (Local state)         │   │
│  │  Data Fetching: Native fetch() API                                  │   │
│  │  Notifications: Sonner (Toast library)                              │   │
│  │  Styling: Tailwind CSS 4.x + Custom animations                      │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  Authentication Layer: Clerk (Middleware)                                   │
│  ├─ Session management                                                      │
│  ├─ Token validation                                                        │
│  └─ Redirect to /sign-in if unauthenticated                                │
│                                                                              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               │ HTTP/JSON
                               │ Clerk JWT Token in Headers
                               │
┌──────────────────────────────▼──────────────────────────────────────────────┐
│                       API ROUTES LAYER (Next.js API)                         │
│                       /api/* (Server-side handlers)                          │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                      AI GENERATION ROUTES                           │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  POST /api/ai/generate-post                              │     │   │
│  │  │  ├─ Input: { topic, tone, language }                     │     │   │
│  │  │  ├─ Auth: Clerk JWT validation                           │     │   │
│  │  │  ├─ Credit check: Verify user has credits               │     │   │
│  │  │  ├─ Call: Anthropic Claude API                           │     │   │
│  │  │  ├─ Deduct: -1 credit via transaction                    │     │   │
│  │  │  ├─ Return: { content, hashtags, bestTime }              │     │   │
│  │  │  └─ Error: Rollback credit if failed                     │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  POST /api/ai/generate-post-stream                       │     │   │
│  │  │  ├─ Same as above but with streaming response            │     │   │
│  │  │  ├─ Uses Server-Sent Events (SSE)                        │     │   │
│  │  │  └─ Real-time text streaming to client                   │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                      POST MANAGEMENT ROUTES                         │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  GET /api/posts                                          │     │   │
│  │  │  ├─ Auth: Verify Clerk session                           │     │   │
│  │  │  ├─ Query: Find all posts by userId                      │     │   │
│  │  │  ├─ Sort: createdAt DESC                                 │     │   │
│  │  │  └─ Return: Array<Post>                                  │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  PUT /api/posts/[id]                                     │     │   │
│  │  │  ├─ Auth: Verify ownership (post.userId === user.id)     │     │   │
│  │  │  ├─ Validate: Zod schema (content 1-500 chars)           │     │   │
│  │  │  ├─ Update: content, scheduledAt, status                 │     │   │
│  │  │  └─ Return: Updated Post                                 │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  DELETE /api/posts/[id]                                  │     │   │
│  │  │  ├─ Auth: Verify ownership                               │     │   │
│  │  │  ├─ Delete: Remove post from DB                          │     │   │
│  │  │  └─ Return: { success: true, id }                        │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                      CREDIT MANAGEMENT ROUTES                       │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  GET /api/credits/summary                                │     │   │
│  │  │  ├─ Query: User credits + balance                        │     │   │
│  │  │  └─ Return: { credits, transactions[] }                  │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  GET /api/credits/transactions                           │     │   │
│  │  │  ├─ Query: All credit transactions by userId             │     │   │
│  │  │  └─ Return: Transaction history                          │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                      STRIPE PAYMENT ROUTES                          │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  POST /api/stripe/create-checkout-session               │     │   │
│  │  │  ├─ Input: { priceId, credits }                          │     │   │
│  │  │  ├─ Create: Stripe Checkout Session                      │     │   │
│  │  │  └─ Return: { url, sessionId }                           │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  GET /api/stripe/session/[sessionId]                     │     │   │
│  │  │  ├─ Query: Stripe session status                         │     │   │
│  │  │  └─ Return: Payment status + details                     │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  POST /api/stripe/webhook                                │     │   │
│  │  │  ├─ Listen: Stripe webhook events                        │     │   │
│  │  │  ├─ Verify: Webhook signature                            │     │   │
│  │  │  ├─ Handle: checkout.session.completed                   │     │   │
│  │  │  ├─ Add: Credits to user account                         │     │   │
│  │  │  └─ Create: CreditTransaction + StripeTransaction        │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                      AUTHENTICATION ROUTES                          │   │
│  │  ┌──────────────────────────────────────────────────────────┐     │   │
│  │  │  POST /api/webhooks/clerk                                │     │   │
│  │  │  ├─ Listen: Clerk webhook events                         │     │   │
│  │  │  ├─ Handle: user.created                                 │     │   │
│  │  │  ├─ Create: User in database                             │     │   │
│  │  │  └─ Initialize: 10 free credits                          │     │   │
│  │  └──────────────────────────────────────────────────────────┘     │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└──────────────────────────────┬──────────────────────────────────────────────┘
                               │
                               │ Prisma ORM (Type-safe DB client)
                               │
┌──────────────────────────────▼──────────────────────────────────────────────┐
│                    DATABASE LAYER (PostgreSQL)                               │
│                    Managed via Prisma Schema                                 │
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐   │
│  │                          TABLES (Models)                            │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐      │   │
│  │  │  User                                                    │      │   │
│  │  │  ├─ id: String (cuid)                                    │      │   │
│  │  │  ├─ clerkId: String (unique, indexed)                    │      │   │
│  │  │  ├─ email: String (unique, indexed)                      │      │   │
│  │  │  ├─ credits: Int (default: 10)                           │      │   │
│  │  │  ├─ organizationId: String? (for Team plan)              │      │   │
│  │  │  ├─ createdAt: DateTime                                  │      │   │
│  │  │  └─ updatedAt: DateTime                                  │      │   │
│  │  │                                                           │      │   │
│  │  │  Relations:                                               │      │   │
│  │  │  ├─ posts[] (One-to-Many)                                │      │   │
│  │  │  ├─ creditTransactions[] (One-to-Many)                   │      │   │
│  │  │  └─ stripeTransactions[] (One-to-Many)                   │      │   │
│  │  └─────────────────────────────────────────────────────────┘      │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐      │   │
│  │  │  Post                                                    │      │   │
│  │  │  ├─ id: String (cuid)                                    │      │   │
│  │  │  ├─ userId: String (FK → User.id, indexed)               │      │   │
│  │  │  ├─ content: String (max 500 chars)                      │      │   │
│  │  │  ├─ platform: String (default: "linkedin")               │      │   │
│  │  │  ├─ status: String (draft/scheduled/published/failed)    │      │   │
│  │  │  ├─ scheduledAt: DateTime? (Timestamptz, indexed) ⭐      │      │   │
│  │  │  ├─ metadata: Json (hashtags, bestTime, etc.)            │      │   │
│  │  │  ├─ createdAt: DateTime (indexed)                        │      │   │
│  │  │  └─ updatedAt: DateTime                                  │      │   │
│  │  │                                                           │      │   │
│  │  │  Relations:                                               │      │   │
│  │  │  └─ user: User (Many-to-One)                             │      │   │
│  │  │                                                           │      │   │
│  │  │  Cascade Delete: ON DELETE CASCADE (user deleted)        │      │   │
│  │  └─────────────────────────────────────────────────────────┘      │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐      │   │
│  │  │  CreditTransaction                                       │      │   │
│  │  │  ├─ id: String (cuid)                                    │      │   │
│  │  │  ├─ userId: String (FK → User.id, indexed)               │      │   │
│  │  │  ├─ amount: Int (negative for deduction, positive for add)│     │   │
│  │  │  ├─ type: String (generation/purchase/refund/bonus)      │      │   │
│  │  │  ├─ description: String                                  │      │   │
│  │  │  ├─ balanceAfter: Int                                    │      │   │
│  │  │  ├─ metadata: Json (Stripe sessionId, postId, etc.)      │      │   │
│  │  │  └─ createdAt: DateTime (indexed)                        │      │   │
│  │  │                                                           │      │   │
│  │  │  Relations:                                               │      │   │
│  │  │  └─ user: User (Many-to-One)                             │      │   │
│  │  └─────────────────────────────────────────────────────────┘      │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐      │   │
│  │  │  StripeTransaction                                       │      │   │
│  │  │  ├─ id: String (cuid)                                    │      │   │
│  │  │  ├─ userId: String (FK → User.id, indexed)               │      │   │
│  │  │  ├─ stripeSessionId: String (unique, indexed)            │      │   │
│  │  │  ├─ stripePaymentId: String?                             │      │   │
│  │  │  ├─ amount: Int (in cents)                               │      │   │
│  │  │  ├─ currency: String (default: "usd")                    │      │   │
│  │  │  ├─ credits: Int (credits purchased)                     │      │   │
│  │  │  ├─ status: String (pending/completed/failed/refunded)   │      │   │
│  │  │  ├─ metadata: Json                                       │      │   │
│  │  │  ├─ createdAt: DateTime (indexed)                        │      │   │
│  │  │  └─ updatedAt: DateTime                                  │      │   │
│  │  │                                                           │      │   │
│  │  │  Relations:                                               │      │   │
│  │  │  └─ user: User (Many-to-One)                             │      │   │
│  │  └─────────────────────────────────────────────────────────┘      │   │
│  │                                                                      │   │
│  │  ┌─────────────────────────────────────────────────────────┐      │   │
│  │  │  PricingPlan                                             │      │   │
│  │  │  ├─ id: String (cuid)                                    │      │   │
│  │  │  ├─ name: String (unique) (Starter/Popular/Pro)          │      │   │
│  │  │  ├─ stripePriceId: String (unique)                       │      │   │
│  │  │  ├─ stripeProductId: String                              │      │   │
│  │  │  ├─ credits: Int                                         │      │   │
│  │  │  ├─ priceInCents: Int                                    │      │   │
│  │  │  ├─ currency: String (default: "usd")                    │      │   │
│  │  │  ├─ isActive: Boolean (default: true, indexed)           │      │   │
│  │  │  ├─ isPopular: Boolean (default: false)                  │      │   │
│  │  │  ├─ displayOrder: Int                                    │      │   │
│  │  │  ├─ createdAt: DateTime                                  │      │   │
│  │  │  └─ updatedAt: DateTime                                  │      │   │
│  │  └─────────────────────────────────────────────────────────┘      │   │
│  │                                                                      │   │
│  └────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  Database Indexes:                                                          │
│  ├─ User: clerkId, email, organizationId                                   │
│  ├─ Post: userId, status, createdAt, scheduledAt ⭐                         │
│  ├─ CreditTransaction: userId, type, createdAt                             │
│  ├─ StripeTransaction: userId, stripeSessionId, status, createdAt          │
│  └─ PricingPlan: isActive                                                  │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│                        EXTERNAL SERVICES                                      │
│                                                                               │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │  Clerk Authentication (clerk.com)                                  │    │
│  │  ├─ User sign-up and sign-in                                       │    │
│  │  ├─ Session management                                             │    │
│  │  ├─ JWT token generation                                           │    │
│  │  ├─ Webhook: user.created → Sync to DB                             │    │
│  │  └─ Middleware: Protect all /dashboard routes                      │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │  Anthropic Claude API (api.anthropic.com)                          │    │
│  │  ├─ Model: claude-sonnet-4-5                                       │    │
│  │  ├─ Tool calling: Auto-generate hashtags & best time               │    │
│  │  ├─ Streaming: Real-time text generation                           │    │
│  │  └─ Rate limits: Handled with backoff                              │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
│  ┌────────────────────────────────────────────────────────────────────┐    │
│  │  Stripe Payment Gateway (stripe.com)                               │    │
│  │  ├─ Checkout Session creation                                      │    │
│  │  ├─ Webhook: checkout.session.completed                            │    │
│  │  ├─ Signature verification                                         │    │
│  │  └─ Payment status tracking                                        │    │
│  └────────────────────────────────────────────────────────────────────┘    │
│                                                                               │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Complete User Flow Examples

### Flow 1: User generates a LinkedIn post with AI

```
┌────────────┐
│   User     │
└─────┬──────┘
      │ 1. Navigates to /dashboard/generate
      │
      ▼
┌─────────────────────────────┐
│  Generate Page Component    │
│  (Client-side React)        │
└─────┬───────────────────────┘
      │ 2. Fills form:
      │    - Topic: "AI trends 2025"
      │    - Tone: "Professional"
      │    - Language: "English"
      │ 3. Clicks "Generate"
      │
      ▼
┌─────────────────────────────┐
│  POST /api/ai/generate-post │
│  (API Route Handler)        │
└─────┬───────────────────────┘
      │ 4. Validate JWT (Clerk middleware)
      │ 5. Extract clerkId from token
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Find User          │
│  WHERE clerkId = ...        │
└─────┬───────────────────────┘
      │ 6. Check user.credits >= 1
      │ 7. If credits < 1 → Error 403
      │
      ▼
┌─────────────────────────────┐
│  Call Claude API            │
│  (Anthropic)                │
│  - Prompt: topic + tone     │
│  - Tools: hashtags, bestTime│
└─────┬───────────────────────┘
      │ 8. Claude returns:
      │    {
      │      content: "AI is transforming...",
      │      hashtags: ["#AI", "#Tech"],
      │      bestTime: "Tuesday 10:00 AM"
      │    }
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Create Post        │
│  - userId: user.id          │
│  - content: "AI is..."      │
│  - status: "draft"          │
│  - metadata: { hashtags }   │
└─────┬───────────────────────┘
      │ 9. Post created
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Deduct Credit      │
│  - Update user.credits -= 1 │
│  - Create CreditTransaction │
│    - type: "generation"     │
│    - amount: -1             │
│    - balanceAfter: 9        │
└─────┬───────────────────────┘
      │ 10. Return to client:
      │     { success: true, post, creditsLeft: 9 }
      │
      ▼
┌─────────────────────────────┐
│  Generate Page Component    │
│  - Shows generated content  │
│  - Displays success toast   │
│  - Updates credit counter   │
└─────────────────────────────┘
```

---

### Flow 2: User reschedules a post via drag-and-drop calendar

```
┌────────────┐
│   User     │
└─────┬──────┘
      │ 1. Navigates to /dashboard/posts
      │ 2. Switches to Calendar view
      │
      ▼
┌─────────────────────────────┐
│  GET /api/posts             │
│  (Fetch all posts)          │
└─────┬───────────────────────┘
      │ 3. Authenticate with Clerk
      │ 4. Find user by clerkId
      │ 5. Query: Post.findMany({ userId })
      │ 6. Return: [{ id, content, scheduledAt, ... }]
      │
      ▼
┌─────────────────────────────┐
│  CalendarView Component     │
│  - Renders react-big-calendar│
│  - Maps posts to events     │
│  - Event: scheduledAt date  │
└─────┬───────────────────────┘
      │ 7. User drags post from
      │    Jan 10 → Jan 15
      │
      ▼
┌─────────────────────────────┐
│  handleEventDrop()          │
│  (Optimistic UI update)     │
│  - Move event on calendar   │
│  - Show as new date         │
└─────┬───────────────────────┘
      │ 8. Call API in background
      │
      ▼
┌─────────────────────────────┐
│  PUT /api/posts/[id]        │
│  Body: {                    │
│    scheduledAt: "2025-01-15"│
│  }                          │
└─────┬───────────────────────┘
      │ 9. Authenticate with Clerk
      │ 10. Verify post ownership
      │     (post.userId === user.id)
      │ 11. Validate date (Zod)
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Update Post        │
│  WHERE id = postId          │
│  SET scheduledAt = new Date │
└─────┬───────────────────────┘
      │ 12. Return: { success: true, updatedPost }
      │
      ▼
┌─────────────────────────────┐
│  CalendarView Component     │
│  - Show success toast       │
│  - "Post rescheduled to     │
│     January 15, 2025"       │
│                             │
│  IF ERROR:                  │
│  - Revert event position    │
│  - Show error toast         │
│  - "Failed to reschedule"   │
└─────────────────────────────┘
```

---

### Flow 3: User purchases credits with Stripe

```
┌────────────┐
│   User     │
└─────┬──────┘
      │ 1. Navigates to /dashboard/credits/purchase
      │ 2. Selects "Popular Plan" (50 credits, $29)
      │ 3. Clicks "Purchase"
      │
      ▼
┌─────────────────────────────────────┐
│  POST /api/stripe/create-checkout-  │
│  session                            │
└─────┬───────────────────────────────┘
      │ 4. Authenticate with Clerk
      │ 5. Find user by clerkId
      │ 6. Find pricing plan by priceId
      │
      ▼
┌─────────────────────────────┐
│  Stripe API Call            │
│  stripe.checkout.sessions   │
│  .create({                  │
│    line_items: [{           │
│      price: stripePriceId,  │
│      quantity: 1            │
│    }],                      │
│    metadata: {              │
│      userId: user.id,       │
│      credits: 50            │
│    },                       │
│    success_url,             │
│    cancel_url               │
│  })                         │
└─────┬───────────────────────┘
      │ 7. Stripe returns:
      │    { id: "cs_xxx", url: "checkout.stripe.com/..." }
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Create             │
│  StripeTransaction          │
│  - stripeSessionId: cs_xxx  │
│  - status: "pending"        │
│  - amount: 2900 (cents)     │
│  - credits: 50              │
└─────┬───────────────────────┘
      │ 8. Return to client:
      │    { url: "checkout.stripe.com/..." }
      │
      ▼
┌─────────────────────────────┐
│  Browser redirects to       │
│  Stripe Checkout page       │
└─────┬───────────────────────┘
      │ 9. User enters card details
      │ 10. Completes payment
      │
      ▼
┌─────────────────────────────┐
│  Stripe sends webhook       │
│  POST /api/stripe/webhook   │
│  Event: checkout.session.   │
│         completed           │
└─────┬───────────────────────┘
      │ 11. Verify webhook signature
      │     (STRIPE_WEBHOOK_SECRET)
      │ 12. Extract sessionId
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Find               │
│  StripeTransaction          │
│  WHERE stripeSessionId =... │
└─────┬───────────────────────┘
      │ 13. Transaction found
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Update User        │
│  user.credits += 50         │
│  (from 10 → 60)             │
└─────┬───────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Create             │
│  CreditTransaction          │
│  - type: "purchase"         │
│  - amount: +50              │
│  - balanceAfter: 60         │
│  - metadata: { sessionId }  │
└─────┬───────────────────────┘
      │
      ▼
┌─────────────────────────────┐
│  Prisma: Update             │
│  StripeTransaction          │
│  - status: "completed"      │
│  - stripePaymentId: pi_xxx  │
└─────┬───────────────────────┘
      │ 14. Webhook returns 200 OK
      │
      ▼
┌─────────────────────────────┐
│  Stripe redirects to        │
│  /dashboard/credits/success │
│  - Shows success message    │
│  - "50 credits added!"      │
└─────────────────────────────┘
```

---

## 🔐 Authentication Flow (Clerk Integration)

```
┌────────────┐
│   User     │
│ (New user) │
└─────┬──────┘
      │ 1. Clicks "Sign Up"
      │
      ▼
┌─────────────────────────────┐
│  /sign-up (Clerk UI)        │
│  - Email + Password         │
│  - OR Social OAuth          │
└─────┬───────────────────────┘
      │ 2. User creates account
      │
      ▼
┌─────────────────────────────┐
│  Clerk creates user         │
│  - Generates clerkId        │
│  - Stores in Clerk DB       │
│  - Sends webhook            │
└─────┬───────────────────────┘
      │ 3. POST /api/webhooks/clerk
      │    Event: user.created
      │    Data: { id, email, ... }
      │
      ▼
┌─────────────────────────────┐
│  Webhook Handler            │
│  - Verify webhook signature │
│  - Extract user data        │
└─────┬───────────────────────┘
      │ 4. Create user in our DB
      │
      ▼
┌─────────────────────────────┐
│  Prisma: User.create({      │
│    clerkId: evt.data.id,    │
│    email: evt.data.email,   │
│    credits: 10  ← Free!     │
│  })                         │
└─────┬───────────────────────┘
      │ 5. User saved in PostgreSQL
      │
      ▼
┌─────────────────────────────┐
│  Clerk redirects to         │
│  /dashboard                 │
│  - JWT token in cookie      │
│  - Session active           │
└─────────────────────────────┘

─────────────────────────────────

For existing users (Login):

┌────────────┐
│   User     │
│ (Existing) │
└─────┬──────┘
      │ 1. Clicks "Sign In"
      │
      ▼
┌─────────────────────────────┐
│  /sign-in (Clerk UI)        │
│  - Email + Password         │
└─────┬───────────────────────┘
      │ 2. Clerk authenticates
      │ 3. Issues JWT token
      │
      ▼
┌─────────────────────────────┐
│  Middleware (middleware.ts) │
│  - Checks JWT on every req  │
│  - Extracts clerkId         │
│  - Attaches to request      │
└─────┬───────────────────────┘
      │ 4. User accesses /dashboard
      │
      ▼
┌─────────────────────────────┐
│  Dashboard Page             │
│  - auth() from @clerk/next  │
│  - Gets userId (clerkId)    │
│  - Queries DB by clerkId    │
│  - Renders user data        │
└─────────────────────────────┘
```

---

## 📊 Data Flow Summary

### Read Operations (GET)
```
Browser → Next.js Page → API Route → Clerk Auth → Prisma → PostgreSQL
                                                      ↓
Browser ← JSON Response ← API Route ← Prisma ← Query Result
```

### Write Operations (POST/PUT/DELETE)
```
Browser → Next.js Page → API Route → Clerk Auth → Validate (Zod)
                                                      ↓
                                              Prisma Transaction
                                              ↓           ↓
                                         Update DB  Create Logs
                                              ↓
Browser ← JSON Response ← API Route ← Success/Error
```

### External Service Calls
```
API Route → Anthropic Claude → Generate Text → Return to API
API Route → Stripe Checkout → Create Session → Return URL
Stripe → Webhook → /api/stripe/webhook → Update DB → 200 OK
```

---

## 🎯 Key Technologies

| Layer | Technology | Purpose |
|-------|-----------|---------|
| Frontend | Next.js 16 (App Router) | React framework with SSR |
| UI Library | React 19 | Component-based UI |
| Styling | Tailwind CSS 4.x | Utility-first CSS |
| UI Components | shadcn/ui + Radix UI | Pre-built accessible components |
| State Management | React useState/useEffect | Local state (no Redux needed) |
| Calendar | react-big-calendar | Event calendar with drag-and-drop |
| Notifications | Sonner | Toast notifications |
| Backend | Next.js API Routes | Serverless API handlers |
| Database ORM | Prisma | Type-safe database client |
| Database | PostgreSQL | Relational database |
| Authentication | Clerk | User auth + session management |
| AI Generation | Anthropic Claude API | Text generation with tool calling |
| Payments | Stripe | Payment processing |
| Date Handling | date-fns | Date formatting and manipulation |
| Validation | Zod | Schema validation |
| Testing | Playwright | E2E testing |

---

## 🔄 State Management Patterns

### Client-side State (React)
```typescript
// Local component state
const [posts, setPosts] = useState<Post[]>([]);
const [isLoading, setIsLoading] = useState(true);

// Fetch data on mount
useEffect(() => {
  fetchPosts();
}, []);

// Optimistic updates
const handleCalendarDrop = (postId, newDate) => {
  // 1. Update UI immediately
  setPosts(prev => prev.map(p =>
    p.id === postId ? { ...p, scheduledAt: newDate } : p
  ));

  // 2. Call API
  try {
    await updatePost(postId, { scheduledAt: newDate });
    toast.success('Post rescheduled');
  } catch {
    // 3. Revert on error
    fetchPosts(); // Refetch from DB
    toast.error('Failed to reschedule');
  }
};
```

### Server-side State (Database)
- Single source of truth: PostgreSQL
- No global state library (Redux/Zustand) needed
- Data fetched fresh on page load
- Mutations go through API → DB → Refresh UI

---

## 📝 Summary

This architecture follows modern best practices:

✅ **Separation of concerns**: Frontend, API, Database clearly separated
✅ **Type safety**: TypeScript + Prisma for end-to-end types
✅ **Authentication**: Clerk handles all auth complexity
✅ **Payments**: Stripe Checkout + webhooks for reliable processing
✅ **AI Integration**: Claude API for content generation
✅ **Optimistic UI**: Fast user experience with error handling
✅ **Database indexing**: Efficient queries on all critical fields
✅ **Error handling**: Try-catch blocks, rollbacks, user feedback
✅ **Testing**: Playwright E2E tests for critical flows

**All data flows through the API layer, ensuring security, validation, and logging at every step.**
